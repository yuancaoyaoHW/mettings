# =============================================================================
# Qwen3-30B-A3B 生产环境配置
# 适用于自部署大模型场景，充分利用模型能力
# =============================================================================

# -----------------------------------------------------------------------------
# Milvus 向量库配置
# -----------------------------------------------------------------------------
MILVUS_URI=http://localhost:19530
MILVUS_TOKEN=
MILVUS_DB_NAME=smart_minutes
MILVUS_COLLECTION_NAME=minutes_v2

# -----------------------------------------------------------------------------
# Embedding 服务配置
# -----------------------------------------------------------------------------
# 可以使用本地 Embedding 模型或与 Qwen3 同一服务器
EMBEDDING_BASE_URL=http://localhost:8080
EMBEDDING_MODEL_NAME=BAAI/bge-m3

# -----------------------------------------------------------------------------
# LLM 服务配置 - Qwen3-30B-A3B 自部署
# -----------------------------------------------------------------------------
# API Key（自部署可随意设置或留空）
LLM_API_KEY=sk-qwen3-local

# Qwen3-30B-A3B 服务地址
# 使用 vLLM / TGI / Xinference 等框架部署
LLM_BASE_URL=http://localhost:8000/v1

# 模型名称（与部署时的模型名一致）
LLM_MODEL_NAME=Qwen3-30B-A3B

# -----------------------------------------------------------------------------
# Rerank 服务配置（可选，用于精排）
# -----------------------------------------------------------------------------
RERANK_BASE_URL=http://localhost:8081
RERANK_MODEL_NAME=BAAI/bge-reranker-v2-m3

# -----------------------------------------------------------------------------
# 智能纪要模块配置
# -----------------------------------------------------------------------------

# 上下文 token 上限（Qwen3 支持 128K 上下文，可适当增大）
CONTEXT_TOKEN_BUDGET=16000

# 默认召回条数
DEFAULT_TOP_K=5

# 字符/token 换算比（中文约 4）
CHARS_PER_TOKEN=4

# 会议类型模板配置
SMART_MINUTES_TEMPLATES_PATH=config/templates.json

# -----------------------------------------------------------------------------
# 映射表数据库（用于口头称呼→正式名映射）
# -----------------------------------------------------------------------------
MAPPING_DB_URI=mysql://user:pass@localhost:3306/smart_minutes

# -----------------------------------------------------------------------------
# Qwen3-30B-A3B 专属优化配置
# -----------------------------------------------------------------------------

# 是否默认使用 LLM 生成扩展字段（自部署模型设为 true）
ENABLE_LLM_FIELD_GENERATION=true

# LLM 字段生成并发数（根据 GPU 资源调整）
LLM_GENERATION_WORKERS=4

# LLM 请求超时时间（秒）
LLM_TIMEOUT=120

# LLM 重试次数
LLM_MAX_RETRIES=3

# -----------------------------------------------------------------------------
# 日志配置
# -----------------------------------------------------------------------------
LOG_LEVEL=INFO
LOG_FILE=logs/smart_minutes.log
