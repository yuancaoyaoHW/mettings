"""Unified LLM client (placeholder)."""
from typing import Optional


def complete(prompt: str, *, model: str = "", api_key: str = "", base_url: Optional[str] = None) -> str:
    """Call LLM for completion. Placeholder returns empty."""
    return ""
